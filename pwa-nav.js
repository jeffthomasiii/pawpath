(()=>{const BASE=document.querySelector('meta[name="pawpath-base"]')?.content||'./';const key='pawpath.activeCarePlan.v1';window.PawPathPwa={base:BASE,readPlan(){try{return JSON.parse(localStorage.getItem(key)||'null')}catch{return null}},writePlan(plan){localStorage.setItem(key,JSON.stringify(plan));}};if('serviceWorker'in navigator)window.addEventListener('load',()=>navigator.serviceWorker.register(`${BASE}sw.js`).catch(()=>{}));document.querySelectorAll('[data-saved-summary]').forEach(el=>{const p=window.PawPathPwa.readPlan();if(!p){el.innerHTML='<h3>No saved plan yet</h3><p>Choose a destination, then save Primary and Backup care options before you leave.</p>';return}const name=p.tripName||p.name||'Your saved trip';const dest=p.tripDestination||p.destination||'Destination saved';el.innerHTML=`<p class="pp-kicker">Your next adventure</p><h2>${escapeHtml(name)}</h2><p>${escapeHtml(dest)}</p><a class="pp-button primary" href="${BASE}saved/">Open saved plan →</a>`;});function escapeHtml(s){return String(s).replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));}})();