(()=>{if(!document.querySelector('#care-map')||!window.L||!window.PawPathPwa)return;const NORMAL=16000,EXPANDED=48280,map=L.map('care-map').setView([39,-98],4);L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap'}).addTo(map);let marks=[],items=[],center=null,active=null,lastFocus=null;const status=document.querySelector('#care-status'),results=document.querySelector('#care-results'),notice=document.querySelector('#expanded-care-notice'),sheet=document.querySelector('#facility-sheet');const esc=s=>String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function miles(a,b,c,d){const r=3958.8,p=x=>x*Math.PI/180,dl=p(c-a),dn=p(d-b),q=Math.sin(dl/2)**2+Math.cos(p(a))*Math.cos(p(c))*Math.sin(dn/2)**2;return 2*r*Math.asin(Math.sqrt(q))}function evidence(t,n){const text=[n,t.operator,t.brand,t.description,t.note].filter(Boolean).join(' ').toLowerCase(),em=String(t.emergency||t['veterinary:emergency']||'').toLowerCase(),sp=String(t['healthcare:speciality']||'').toLowerCase(),hours=String(t.opening_hours||'').toLowerCase();if(em==='yes'||sp.includes('emergency'))return{type:'emergency',confidence:'source-listed',note:'The source record explicitly indicates emergency veterinary capability.'};if(sp.includes('urgent')||/\burgent\b/.test(text))return{type:'urgent',confidence:'likely-emergency',note:'Urgent-care capability is inferred from source details.'};if(/\bemergency\b|\b24[ -]?hour\b|\b24\/7\b/.test(text)||/24\/7|00:00-24:00|00:00-00:00/.test(hours))return{type:'emergency',confidence:'likely-emergency',note:'Emergency capability is inferred from listing details or hours.'};return null}function normalize(x,lat,lon,expanded=false){const la=x.lat??x.center?.lat,lo=x.lon??x.center?.lon;if(!Number.isFinite(la)||!Number.isFinite(lo))return null;const t=x.tags||{},name=t.name||t.operator||'Veterinary facility',phone=t.phone||t['contact:phone']||'',address=[[t['addr:housenumber'],t['addr:street']].filter(Boolean).join(' '),[t['addr:city'],t['addr:state'],t['addr:postcode']].filter(Boolean).join(', ')].filter(Boolean).join(' • '),ev=evidence(t,name),type=ev?.type||'routine';return{id:`${x.type}-${x.id}`,name,address,phone,website:t.website||t['contact:website']||'',hours:t.opening_hours||'',careType:type,confidence:ev?.confidence||(!phone||!address?'needs-confirmation':'source-listed'),source:'OpenStreetMap',sourceUrl:`https://www.openstreetmap.org/${x.type}/${x.id}`,sourceNotes:ev?`${ev.note} Call ahead to confirm current capability and availability.`:'Listing does not explicitly establish emergency capability. Confirm directly with the facility.',lat:la,lng:lo,distanceMiles:miles(lat,lon,la,lo),isExtendedEmergencySearch:expanded}}async function query(lat,lon,radius){const q=`[out:json][timeout:25];(node["amenity"="veterinary"](around:${radius},${lat},${lon});way["amenity"="veterinary"](around:${radius},${lat},${lon});relation["amenity"="veterinary"](around:${radius},${lat},${lon}););out center tags;`;const r=await fetch('https://overpass-api.de/api/interpreter',{method:'POST',body:new URLSearchParams({data:q})});if(!r.ok)throw Error(`Overpass ${r.status}`);return(await r.json()).elements}function clear(){marks.forEach(m=>m.remove());marks=[];results.innerHTML=''}function select(role,item){const p=PawPathPwa.readPlan();if(!p?.trip?.name||!p?.trip?.destination){status.innerHTML='Save your trip essentials first before choosing Primary or Backup. <a href="../plan/">Open Plan a Trip</a>.';return}if(role==='primary'&&!['emergency','urgent'].includes(item.careType)){document.querySelector('#sheet-status').textContent='Primary requires a listing with emergency or urgent-care evidence. This facility can still be Backup.';return}const other=role==='primary'?'backup':'primary';if(p.facilities[other]?.id===item.id){document.querySelector('#sheet-status').textContent='Primary and Backup must be different facilities.';return}PawPathPwa.updatePlan(plan=>{plan.facilities[role]={...item}});document.querySelector('#sheet-status').textContent=`Saved as ${role}. Call ahead to confirm current capability and availability.`;render();openSheet(item)}function render(){clear();const p=PawPathPwa.readPlan();items.forEach(item=>{marks.push(L.marker([item.lat,item.lng]).addTo(map).bindPopup(`<strong>${esc(item.name)}</strong>`));const card=document.createElement('article');card.className='care-result'+(item.isExtendedEmergencySearch?' is-expanded':'');card.tabIndex=0;card.innerHTML=`<div class="care-result-summary"><div><h3>${esc(item.name)}</h3><span class="care-badge">${esc(item.isExtendedEmergencySearch?'Expanded emergency option':item.careType)}</span></div><span class="care-result-more">Details ›</span></div><p>${esc(item.address||'Address not listed')}</p><p>${item.distanceMiles.toFixed(1)} mi away${p?.facilities?.primary?.id===item.id?' · Primary ✓':p?.facilities?.backup?.id===item.id?' · Backup ✓':''}</p>`;card.onclick=()=>openSheet(item);card.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();openSheet(item)}};results.append(card)})}function openSheet(item){active=item;lastFocus=document.activeElement;const p=PawPathPwa.readPlan(),directions=`https://www.google.com/maps/dir/?api=1&destination=${item.lat},${item.lng}`;document.querySelector('#sheet-content').innerHTML=`<span class="care-badge">${esc(item.isExtendedEmergencySearch?'Expanded emergency option':item.careType)}</span><h2>${esc(item.name)}</h2><p>${esc(item.sourceNotes)}</p><div class="facility-sheet-meta"><div><span>Distance</span><strong>${item.distanceMiles.toFixed(1)} miles from search</strong></div><div><span>Address</span><strong>${esc(item.address||'Not listed')}</strong></div><div><span>Phone</span><strong>${esc(item.phone||'Not listed')}</strong></div><div><span>Hours</span><strong>${esc(item.hours||'Not listed')}</strong></div><div><span>Confidence</span><strong>${esc(item.confidence==='source-listed'?'Source-listed details; still confirm by phone':item.confidence==='likely-emergency'?'Emergency capability inferred; confirm by phone':'Important details need confirmation')}</strong></div></div><div class="facility-sheet-actions">${item.phone?`<a class="pp-button primary" href="tel:${item.phone.replace(/[^+\d]/g,'')}">Call</a>`:'<span></span>'}<a class="pp-button secondary" href="${directions}" target="_blank" rel="noopener">Directions ↗</a>${item.website?`<a class="pp-button secondary" href="${esc(item.website)}" target="_blank" rel="noopener">Website ↗</a>`:''}<a class="pp-button secondary" href="${item.sourceUrl}" target="_blank" rel="noopener">OSM source ↗</a></div><div class="facility-sheet-role"><button data-sheet-role="primary" class="${p?.facilities?.primary?.id===item.id?'is-selected':''}">${p?.facilities?.primary?.id===item.id?'Primary ✓':'Set Primary'}</button><button data-sheet-role="backup" class="${p?.facilities?.backup?.id===item.id?'is-selected':''}">${p?.facilities?.backup?.id===item.id?'Backup ✓':'Set Backup'}</button></div><p id="sheet-status" class="pp-note" role="status">Primary and Backup must be different facilities.</p>`;document.querySelectorAll('[data-sheet-role]').forEach(b=>b.onclick=()=>select(b.dataset.sheetRole,item));sheet.hidden=false;document.body.style.overflow='hidden';document.querySelector('.facility-sheet-close').focus()}function closeSheet(){sheet.hidden=true;document.body.style.overflow='';lastFocus?.focus?.()}document.querySelector('.facility-sheet-close').onclick=closeSheet;sheet.onclick=e=>{if(e.target===sheet)closeSheet()};document.addEventListener('keydown',e=>{if(e.key==='Escape'&&!sheet.hidden)closeSheet()});async function findCare(lat,lon,label){center={lat,lon};status.textContent='Searching near '+label+'…';notice.hidden=true;items=[];clear();map.setView([lat,lon],12);try{const nearby=(await query(lat,lon,NORMAL)).map(x=>normalize(x,lat,lon)).filter(Boolean).sort((a,b)=>a.distanceMiles-b.distanceMiles);items=nearby.slice(0,30);if(!nearby.some(i=>['emergency','urgent'].includes(i.careType))){status.textContent='No emergency or urgent-care listing appeared nearby. Expanding emergency search to 30 miles…';try{const existing=new Set(nearby.map(i=>i.id)),far=(await query(lat,lon,EXPANDED)).map(x=>normalize(x,lat,lon,true)).filter(i=>i&&!existing.has(i.id)&&['emergency','urgent'].includes(i.careType)&&i.distanceMiles>10&&i.distanceMiles<=30).sort((a,b)=>a.distanceMiles-b.distanceMiles);if(far[0]){items=[...items,far[0]].sort((a,b)=>a.distanceMiles-b.distanceMiles);notice.hidden=false;notice.innerHTML=`<strong>Expanded emergency search</strong>No emergency or urgent-care listing appeared in the normal search area. PawPath added the nearest likely option within 30 miles: ${esc(far[0].name)}, ${far[0].distanceMiles.toFixed(1)} miles away. Call ahead before traveling.`}else{notice.hidden=false;const ext=`https://www.google.com/maps/search/?api=1&query=${encodeURIComponent(`emergency veterinarian near ${lat.toFixed(5)},${lon.toFixed(5)}`)}`;notice.innerHTML=`<strong>Emergency facility not identified</strong>OpenStreetMap did not provide enough evidence to identify emergency or urgent care within 30 miles. <a href="${ext}" target="_blank" rel="noopener">Search emergency veterinarians externally ↗</a>`}}catch{notice.hidden=false;notice.innerHTML='<strong>Expanded search unavailable</strong>The extended emergency search could not be completed. Call ahead and use another veterinary search source if needed.'}}status.textContent=items.length?`${items.length} care options shown. Confirm details directly with the facility.`:'No veterinary facilities were returned for this area.';render()}catch{status.textContent='Live care data could not be loaded. Try again shortly or use another veterinary search source.'}}async function geocode(text){status.textContent='Finding '+text+'…';const r=await fetch('https://nominatim.openstreetmap.org/search?format=json&limit=1&q='+encodeURIComponent(text));if(!r.ok)throw Error();const d=await r.json();if(!d[0])throw Error();return d[0]}document.querySelector('#care-search').addEventListener('submit',async e=>{e.preventDefault();try{const d=await geocode(q.value.trim());findCare(+d.lat,+d.lon,d.display_name)}catch{status.textContent='That location could not be found. Try a city, state, or ZIP code.'}});document.querySelector('#use-location').onclick=()=>navigator.geolocation?navigator.geolocation.getCurrentPosition(p=>findCare(p.coords.latitude,p.coords.longitude,'your location'),()=>status.textContent='Location access was unavailable. Search by city or ZIP code instead.'):(status.textContent='Location is not available in this browser.');const initial=new URLSearchParams(location.search).get('destination');if(initial){q.value=initial;document.querySelector('#care-search').requestSubmit()}})();